#!/bin/bash

set -e

mkdir -p ~/src
cd ~/src

if !(git clone https://github.com/meskarune/i3lock-fancy)
then
  cd i3lock-fancy
  git pull
else
  cd i3lock-fancy
fi

git checkout dualmonitors

sudo make install
